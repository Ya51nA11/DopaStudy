import React from 'react';
import ReactDOM from 'react-dom';
import UploadPage from './components/UploadPage'; // Make sure the path is correct
import './index.css'; // Include global styles if any

ReactDOM.render(
  <React.StrictMode>
    <UploadPage />
  </React.StrictMode>,
  document.getElementById('root')
);